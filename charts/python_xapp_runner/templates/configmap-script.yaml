apiVersion: v1
kind: ConfigMap
metadata:
  name: xapp-script
data:
  {{- $files := .Files }}
  
  # Including specific files in the root directory
  {{- range tuple "xapp.py" "script2.py" }}
  {{ . }}: |-
    {{ $files.Get . | trim | indent 4 }}
  {{- end }}

  # Including all files inside the lib/ directory
  {{- $libFiles := .Files.Glob "lib/*" }}
  {{- range $file := $libFiles }}
  {{ $file.Name | base }}: |
    {{ .Files.Get $file | nindent 8 }}
  {{- end }}
