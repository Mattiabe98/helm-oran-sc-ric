apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-appmgr-deployment
  labels:
    app: appmgr
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: appmgr
  template:
    metadata:
      labels:
        app: appmgr
    spec:
      containers:
        - name: appmgr
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          env:
            {{- range ,  := .Values.env }}
            - name: {{  }}
              value: {{  | quote }}
            {{- end }}
          volumeMounts:
            {{- toYaml .Values.volumeMounts | nindent 12 }}
          volumes:
            {{- toYaml .Values.volumes | nindent 8 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
